# Challenger Living Cover Sky-Masked Ambient C HTML Proof

Review-only full-runtime HTML proof repairing the preferred Ambient C direction.

Status: `tighten`. Human review rejected this mask shape as too rough/wrong for the plate; a successor should use an image-derived visible-sky mask rather than broad geometric polygons.

This packet preserves the single lights-on backplate, fixed `1920x1080` canvas, 760px rail, native 40px WebVTT rail captions, staged rail transitions, approved audio/caption/transcript references, and range-capable review controls.

## What Changed

- Uses a fixed `1920x1080` sky visibility mask so distant aircraft lights disappear behind shuttle, tower, pad, crew, and right-rail foreground areas.
- Increases aircraft coverage to seven distant point-light passes with varied vectors across the full runtime.
- Gives localized dust motes slightly more discernible slow movement while keeping them sparse and masked to the sky/practical-light falloff area.

Open:

- `http://127.0.0.1:8794/player.html`

No MP4 render, final assembly, Shorts work, publish readiness, or YouTube action is authorized by this packet.
